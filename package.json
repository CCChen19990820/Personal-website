{
  "name": "portfolio_nextjs",
  "homepage": "https://ccchen19990820.github.io/personal-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "export": "next export",
    "deploy": "rimraf node_modules/.cache && next build && next export && echo test> out/.nojekyll && git add out/ && git commit -m \"Deploy Next.js to gh-pages\" && git subtree push --prefix out origin gh-pages",
    "start": "next start"
  },
  "dependencies": {
    "next": "13.1.3",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "react-icons": "^4.2.0",
    "styled-components": "^5.3.0",
    "styled-normalize": "^8.0.7"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "rimraf": "^4.1.2",
    "tailwindcss": "^3.2.7"
  }
}
